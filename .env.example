# Copy values from this file into .env if you want to change defaults.
# Docker Compose reads .env automatically, but it does not read this example file.
API_PORT=8000
FRONTEND_PORT=5173
DATABASE_PATH=/data/visits.db
TEST_DATABASE_PATH=/tmp/test-visits.db
VISITS_PER_TREE=5
VITE_API_BASE_URL=http://localhost:8000
